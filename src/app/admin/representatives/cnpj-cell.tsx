// Movido para @/components/shared/cnpj-cell (reutilizado por admin, representante
// e cliente). Shim mantido só porque `rm` está bloqueado no settings — pode ser
// apagado com segurança (sem outros importadores).
export { CnpjCell } from "@/components/shared/cnpj-cell";
